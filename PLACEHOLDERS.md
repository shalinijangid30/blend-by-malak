# Placeholder content — replace before this ever goes live for real

This is a speculative pre-pitch build (see the original brief). Everything
below is a stand-in and should be swapped for Malak's real content once she
engages.

- **WhatsApp number** — `lib/whatsapp.ts` has `974XXXXXXX`. Replace with her
  real WhatsApp Business number.
- **Pricing** — `data/workshops.ts` uses the ~QR170 figure seen in press.
  Confirm current pricing before publishing anything real.
- **Photos** — `public/images/*.jpg` are 3 royalty-free Unsplash paint
  photos used as a stand-in for her actual workshop/Instagram photography.
  Category cards use icon tiles instead of mismatched stock photos.
- **Weekly schedule** — `data/schedule.ts` is fictional (café names, dates,
  themes). This is the file meant to be hand-edited every week; could later
  move to a lightweight CMS field.
- **Press quotes** — `data/pressMentions.ts` / `messages/*.json` `press.*`
  keys are invented text, not real quotes from Time Out Doha / ILoveQatar /
  974 Qatar. Rendered as text badges, not their real logos, to avoid using
  trademarked assets without permission.
- **Café partners** — names in `app/[locale]/about/page.tsx` and
  `app/[locale]/contact/page.tsx` are placeholders, not confirmed partners.
- **Arabic copy** — `messages/ar.json` is a first-pass translation, not
  reviewed by Malak. Confirm she wants translation done vs. providing her
  own copy.
- **Instagram/TikTok handles** — assumed `@blendbymalak`; confirm exact
  handles before publishing links.

**Next step:** hand this list to Malak as the exact content ask once she
engages, per the original brief.
